#rename a branch

git branch -m old_name new_name

#checkout a commit and then create new branch from it

git checkout <hash>
git checkout -b new_branch_name

#delete a remote branch
git push origin --delete branch_name
#another flavor
git push origin :branch_name